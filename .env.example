VITE_CLERK_PUBLISHABLE_KEY=

# Set these in Vercel (Settings > Environment Variables), not in committed files.
RESEND_API_KEY=

# Where signup alerts go (gmail is fine here)
NOTIFY_EMAIL=

# Sender address — must NOT be gmail/yahoo/etc. Use onboarding@resend.dev until you verify your own domain at resend.com/domains
FROM_EMAIL=onboarding@resend.dev

ZOOM_LINK=
